server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/accounts_db
    username: postgres
    password: vasanth

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true